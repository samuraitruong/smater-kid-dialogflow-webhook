export enum Intents {
    WELCOME_YES_FOLLOWUP = "Default Welcome Intent - yes",
    NUMBER = "Number Intent",
    WELCOME = "Default Welcome Intent",
    FALLBACK = "Default Fallback Intent",
    REPEAT = "Repeat Intent",
}
export enum Operators {
    ADD = "+",
    MINUS = "-",
    MUTIPLY = "*",
    DIVIDE = "/",
}
